---
docker_package_repo: "https://download.docker.com/linux/{{ ansible_distribution | lower }}"
docker_package_repo_key: "{{ docker_package_repo }}/gpg"
docker_package_repo_file: "{{ docker_package_repo }}/docker-ce.repo"
