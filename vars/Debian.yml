---
docker_package_prereqs:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg-agent
  - software-properties-common

docker_package_repo: https://download.docker.com/linux/debian
docker_package_repo_key: "{{ docker_package_repo }}/gpg"
